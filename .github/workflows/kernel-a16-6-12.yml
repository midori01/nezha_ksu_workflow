name: Build GKI Android 16 (6.12) Worker

on:
  workflow_call:
    inputs:
      ksu_variant:
        required: true
        type: string
      sub_level:
        required: true
        type: string
      os_patch_level:
        required: true
        type: string
      enable_susfs:
        required: true
        type: boolean
      create_release:
        required: true
        type: boolean

permissions:
  contents: write
  packages: write

jobs:
  build:
    name: Build ${{ inputs.ksu_variant }} 6.12.${{ inputs.sub_level }} (${{ inputs.os_patch_level }})
    runs-on: ubuntu-latest
    timeout-minutes: 300
    env:
      ENABLE_SUSFS: ${{ inputs.enable_susfs }}
      KSU_VARIANT: ${{ inputs.ksu_variant }}
      ANDROID_VERSION: 'android16'
      KERNEL_VERSION: '6.12'
      SUB_LEVEL: ${{ inputs.sub_level }}
      OS_PATCH_LEVEL: ${{ inputs.os_patch_level }}
    steps:
      - uses: actions/checkout@v6
        with:
          clean: false
          submodules: true

      - name: Free Disk Space & Install Dependencies
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
          sudo docker image prune -af 2>/dev/null || true
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends binutils python-is-python3 libssl-dev libelf-dev libdw-dev curl bison flex make git perl gcc bc zip unzip aria2

      - name: Clone Scripts Repo
        run: |
          git clone --depth=1 https://github.com/yapixel/sh "${{ github.workspace }}/sh"
          chmod +x "${{ github.workspace }}/sh/get_ksuver.sh"

      - name: Install Repo Tool
        run: |
          mkdir -p ~/bin
          curl -s https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          sudo ln -sf ~/bin/repo /usr/bin/repo

      - name: Initialize Workspace & Sync Google Source
        run: |
          mkdir -p kernel_workspace && cd kernel_workspace
          FORMATTED_BRANCH="${ANDROID_VERSION}-${KERNEL_VERSION}-${OS_PATCH_LEVEL}"
          repo init -u https://android.googlesource.com/kernel/manifest -b common-${FORMATTED_BRANCH} --depth=1

          REMOTE_BRANCH=$(git ls-remote https://android.googlesource.com/kernel/common ${FORMATTED_BRANCH})
          if grep -q deprecated <<< "$REMOTE_BRANCH"; then
            sed -i "s/\"${FORMATTED_BRANCH}\"/\"deprecated\/${FORMATTED_BRANCH}\"/g" .repo/manifests/default.xml
          fi
          
          repo --trace sync -c -j$(nproc --all) --fail-fast --no-tags --force-sync --no-clone-bundle
          sed -i 's/ -dirty//g' common/scripts/setlocalversion

          aria2c -s16 -x16 -k1M https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang19-r536225/clang-r536225.zip -o clang.zip
          unzip -q clang.zip -d clang19 && rm clang.zip
          aria2c -s16 -x16 -k1M https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang19-r536225/rust.zip -o rust.zip
          unzip -q rust.zip -d rust && rm rust.zip
          aria2c -s16 -x16 -k1M https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang19-r536225/build-tools.zip -o build-tools.zip
          unzip -q build-tools.zip && rm build-tools.zip

      - name: Setup KernelSU Variant
        run: |
          cd kernel_workspace/common
          if [ "${{ inputs.ksu_variant }}" = "kowsu" ]; then
            KSU_REPO="https://github.com/KOWX712/KernelSU.git"
            KSU_USER="KOWX712"
          else
            KSU_REPO="https://github.com/backslashxx/KernelSU.git"
            KSU_USER="backslashxx"
          fi
          git clone --depth=1 --branch "master" $KSU_REPO KernelSU
          ln -sf ../KernelSU/kernel drivers/kernelsu
          if ! grep -q "kernelsu/" drivers/Makefile; then echo 'obj-$(CONFIG_KSU) += kernelsu/' >> drivers/Makefile; fi
          if ! grep -q "drivers/kernelsu/Kconfig" drivers/Kconfig; then sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig; fi
          
          KSU_VER=$("${{ github.workspace }}/sh/get_ksuver.sh" "$KSU_USER" "KernelSU" "master" || echo "unknown")
          echo "KSU_VER=${KSU_VER}" >> $GITHUB_ENV

      - name: Apply SuSFS Patches (KowSU Only)
        if: inputs.enable_susfs == true
        run: |
          SUSFS_DIR="${{ github.workspace }}/susfs4ksu"
          cd kernel_workspace/common
          cp "$SUSFS_DIR/kernel_patches/fs/susfs.c" fs/
          cp "$SUSFS_DIR/kernel_patches/include/linux/susfs.h" include/linux/
          cp "$SUSFS_DIR/kernel_patches/include/linux/susfs_def.h" include/linux/
          patch -p1 < "$SUSFS_DIR/kernel_patches/50_add_susfs_in_gki-android16-6.12.patch"
          cd KernelSU
          patch -p1 -l --fuzz=3 < "${{ github.workspace }}/.github/patches/kowsu/10_enable_susfs_for_ksu.patch"

      - name: Apply Security Hooks (xxKSU Only)
        if: inputs.ksu_variant == 'xxksu'
        run: |
          cd kernel_workspace/common
          # Assuming you have your healing script or manual patches for xxksu here
          patch -p1 -l --fuzz=3 < "${{ github.workspace }}/.github/patches/xxksu/manual-security-hooks-v1.8.patch"
          patch -p1 -l --fuzz=3 < "${{ github.workspace }}/.github/patches/xxksu/scope-min-hooks-v1.9.patch"

      - name: Apply KMI Bypass & Configs
        run: |
          cd kernel_workspace/common
          sed -i '/bad_version:/,/^}/ s/return 0;/return 1;/' kernel/module/version.c
          
          DEFCONFIG="arch/arm64/configs/gki_defconfig"
          echo "CONFIG_KSU=y" >> $DEFCONFIG
          echo "CONFIG_RUST=y" >> $DEFCONFIG
          echo 'CONFIG_LOCALVERSION="-android16-gki"' >> $DEFCONFIG
          
          if [ "${{ inputs.enable_susfs }}" = "true" ]; then
             echo "CONFIG_KSU_SUSFS=y" >> $DEFCONFIG
             # ... add other SuSFS flags as needed ...
          fi

      - name: Compile Kernel
        run: |
          WORKDIR="$(pwd)"
          cd kernel_workspace
          export PATH="$WORKDIR/kernel_workspace/clang19/bin:$WORKDIR/kernel_workspace/build-tools/bin:$WORKDIR/kernel_workspace/rust/bin:$PATH"
          cd common
          if [ "${{ inputs.sub_level }}" != "X" ]; then sed -i "s/^SUBLEVEL = .*/SUBLEVEL = ${{ inputs.sub_level }}/" Makefile; fi
          
          make -j$(nproc --all) O=out LLVM=1 LLVM_IAS=1 ARCH=arm64 gki_defconfig
          make -j$(nproc --all) O=out LLVM=1 LLVM_IAS=1 ARCH=arm64 Image

      - name: Package with AnyKernel3
        run: |
          cd kernel_workspace
          git clone https://github.com/yapixel/AnyKernel3 --depth=1
          cp ./common/out/arch/arm64/boot/Image ./AnyKernel3/
          
          cd AnyKernel3
          ZIP_NAME="${{ inputs.ksu_variant }}_6.12.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}.zip"
          zip -r9 "../$ZIP_NAME" . -x ".git*"
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          # CRITICAL: Unique name using variant, sublevel, and patch date
          name: kernel-${{ inputs.ksu_variant }}-${{ inputs.sub_level }}-${{ inputs.os_patch_level }}
          path: kernel_workspace/${{ env.ZIP_NAME }}
